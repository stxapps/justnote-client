const initialState = {
  href: null,
  historyPosition: null,
  width: null,
  height: null,
  pageYOffset: 0,
};

const windowReducer = (state = initialState, action) => {

  return state;
};

export default windowReducer;
